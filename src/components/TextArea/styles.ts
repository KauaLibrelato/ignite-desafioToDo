import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 327,
    height: 56,
    backgroundColor: "#fff",
    borderRadius: 5,
    paddingHorizontal: 24,
  },
});
